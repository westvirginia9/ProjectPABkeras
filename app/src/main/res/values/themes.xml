<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.ProjectPABkeras" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Gunakan warna hijau kustom -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorSecondary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorError">@color/black</item>
    </style>
    <style name="CustomButtonStyle" parent="Widget.Material3.Button"><!-- Drawable untuk tombol default -->
        <item name="android:backgroundTint">@android:color/transparent</item> <!-- Hilangkan pengaruh tint -->
        <item name="android:textColor">@color/black</item> <!-- Warna teks -->
    </style>

    <style name="Theme.ProjectPABkeras" parent="Base.Theme.ProjectPABkeras" />
</resources>
